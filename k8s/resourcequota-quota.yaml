apiVersion: v1
kind: ResourceQuota
metadata:
  name: akosha-resource-quota
  namespace: akosha
  labels:
    app: akosha-mcp
spec:
  hard:
    requests.cpu: "4"
    requests.memory: "8Gi"
    limits.cpu: "10"
    limits.memory: "20Gi"
    persistentvolumeclaims: "5"
    pods: "20"
